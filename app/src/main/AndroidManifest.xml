<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.autocontrolbrasil.autocontrolbrasil" >

    <uses-feature android:name="android.hardware.camera2" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".PrincipalActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadastrarVeiculoActivity" />
        <activity
            android:name=".InclusaoAbastecimentoActivity"
            android:label="@string/title_activity_inclusao_abastecimento" >
        </activity>
        <activity
            android:name=".ListaAbastecimentosActivity"
            android:label="@string/title_activity_lista_abastecimentos" >
        </activity>
        <activity
            android:name=".SelecionarVeiculoActivity"
            android:label="SelecionarVeiculoActivity">
        </activity>
    </application>

</manifest>
